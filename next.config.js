module.exports = {
  reactStrictMode: true,
  // experimental: {
  //   appDir: false, // Ensure this is disabled for the Pages Router
  // },
  // webpack: (config) => {
  //   config.resolve.alias["@pages"] = path.join(__dirname, "src/apps/pages");
  //   return config;
  // },
};
